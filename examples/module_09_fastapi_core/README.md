# Module 09 Example

这个目录演示 FastAPI 的最小核心能力：路由、Pydantic 请求模型、响应模型和状态码。

建议阅读顺序：

1. `fastapi_tasks_example.py`
2. `test_fastapi_tasks_example.py`

运行方式：

```powershell
uv run pytest examples/module_09_fastapi_core -q
```
