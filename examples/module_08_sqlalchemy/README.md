# Module 08 Example

这个目录演示 SQLAlchemy 2.x 的最小 ORM 流程：建模、建表、插入、查询。

建议阅读顺序：

1. `sqlalchemy_tasks.py`
2. `test_sqlalchemy_tasks.py`

运行方式：

```powershell
uv run pytest examples/module_08_sqlalchemy -q
```
