# Module 05 Example

这个目录演示如何把 `pathlib`、`json`、`datetime`、`uuid`、`enum`、`logging` 组合成一个小工具。

建议阅读顺序：

1. `standard_library_toolkit.py`
2. `test_standard_library_toolkit.py`

运行方式：

```powershell
uv run pytest examples/module_05_standard_library -q
```
